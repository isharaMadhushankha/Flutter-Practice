import 'package:flutter/material.dart';

void main(){
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return  MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Scaffold(
        appBar: AppBar(
          title: Text("ONYX T-Shirt Collection",
          style: TextStyle(
            color: Colors.white,
            
          ),
          ),
          backgroundColor: const Color.fromARGB(255, 8, 6, 99),
          centerTitle: true,
          leading: Icon(
            Icons.menu,
            size: 20,
            color: Colors.white,
          
          ),
          actions: [
            Icon(Icons.search),
            
          ],
        ),
        body: Center(
          child: Text("Welcome again!!",
          style: TextStyle(
            fontFamily: "times New Roman",
            fontSize: 50,
            color: Colors.red
          ),
          ),
         ),
      ),
    );
  }
}
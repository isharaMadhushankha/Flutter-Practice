import 'package:flutter/material.dart';

void main(){
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: SafeArea(
        child: Scaffold(
          appBar: AppBar(
            title: Text("ONYX COLLECTION",style: TextStyle(
              color: Colors.white
            ),),
            backgroundColor: const Color.fromARGB(255, 7, 6, 71),
            leading: Icon(
              Icons.menu,
              color: Colors.white,
            ),
            actions: [
              Icon(
                Icons.search,
                color: Colors.white,
                )
            ],
            centerTitle: true,
          ),
          body: Container(
            color: Colors.amber,
            height: 400,
            width: 400,
            child: Column(
              
              crossAxisAlignment:CrossAxisAlignment.stretch,
              children: [
                Container(
                  color: Colors.red,
                  height: 100,
                  width: 300,
                  child: Text("hello"),
              
                  
                ),
                Container(
                  color: const Color.fromARGB(255, 54, 244, 54),
                  height: 100,
                  width: 300,
                  child: Text("hello"),
                  
                ),
                Container(
                  color: const Color.fromARGB(255, 82, 54, 244),
                  height: 100,
                  width: 300,
                  child: Text("hello"),
                  
                ),
              ],
            ),
          ),
        ),
      ),
    );
  }
}
import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Scaffold(
        appBar: AppBar(
          title: Text("ONYX COLLECTION", style: TextStyle(color: Colors.white)),
          backgroundColor: const Color.fromARGB(255, 5, 9, 58),
          centerTitle: true,

          leading: Icon(Icons.menu, color: Colors.white),
          actions: [
            Icon(color: const Color.fromARGB(255, 255, 255, 255), Icons.search),
          ],
        ),
        body: SingleChildScrollView(
          child: Column(
            children: [
              Row(
                mainAxisAlignment: MainAxisAlignment.center,
                children: [
                  Center(
                    child: Padding(
                      padding: EdgeInsets.all(20),
                      child: Text(
                        "WELCOME AND CHOOSE YOUR'S ONE",
                        style: TextStyle(
                          fontSize: 50,
                          color: const Color.fromARGB(255, 2, 20, 49),
                        ),
                      ),
                    ),
                  ),
                ],
              ),
              SizedBox(height: 20),

              Row(
                mainAxisAlignment: MainAxisAlignment.spaceEvenly,

                children: [
                  Container(
                    height: 200,
                    width: 200,
                    color: Colors.amber,
                    child: Text("product"),
                  ),
                  Container(
                    height: 200,
                    width: 200,
                    color: Colors.amber,
                    child: Text("product"),
                  ),
                  Container(
                    height: 200,
                    width: 200,
                    color: Colors.amber,
                    child: Text("product"),
                  ),
                ],
              ),

              SizedBox(height: 20),

              Row(
                mainAxisAlignment: MainAxisAlignment.spaceEvenly,

                children: [
                  Container(
                    height: 200,
                    width: 200,
                    color: Colors.amber,
                    child: Text("product"),
                  ),
                  Container(
                    height: 200,
                    width: 200,
                    color: Colors.amber,
                    child: Text("product"),
                  ),
                  Container(
                    height: 200,
                    width: 200,
                    color: Colors.amber,
                    child: Text("product"),
                  ),
                ],
              ),
              SizedBox(height: 20),
              Row(
                mainAxisAlignment: MainAxisAlignment.spaceEvenly,

                children: [
                  Container(
                    height: 200,
                    width: 200,
                    color: Colors.amber,
                    child: Text("product"),
                  ),
                  Container(
                    height: 200,
                    width: 200,
                    color: Colors.amber,
                    child: Text("product"),
                  ),
                  Container(
                    height: 200,
                    width: 200,
                    color: Colors.amber,
                    child: Text("product"),
                  ),
                ],
              ),
              SizedBox(height: 20),
            ],
          ),
        ),
      ),
    );
  }
}
